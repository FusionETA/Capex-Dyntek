# Deny all direct web access — these directories hold secrets, tokens, logs
# and application code that must never be served. Only public/ is reachable.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
